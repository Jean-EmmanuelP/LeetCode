#include <stdio.h>
#include <limits.h>

void ft_putstr(struct {a})
{
    printf("%d\n", a);
}

int main(void)
{
    unsigned int  max;

    max = -1;
    printf("%d\n", max);
    printf("%lu\n", sizeof(int));
    ft_putstr({42});
}
// 1000 0000